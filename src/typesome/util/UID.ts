/**
 * Interface to give an object an unique identifier.
 *
 * @author Kevin Palatzky
 * @since 0.1.0
 * @export
 * @interface UID
 */
export interface UID {
	/**
	 *
	 *
	 * @type {string}
	 * @memberOf UID
	 */
	readonly UID: string;
}